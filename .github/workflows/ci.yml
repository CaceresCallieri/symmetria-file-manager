name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  QT_VERSION: "6.9.0"
  KF6_SYNTAX_TAG: "v6.12.0"
  QXLSX_TAG: "v1.5.0"
  POPPLER_TAG: "poppler-25.07.0"
  DEPS_PREFIX: ${{ github.workspace }}/deps

jobs:
  build-and-test:
    runs-on: ubuntu-24.04

    steps:
      - uses: actions/checkout@v4
        with:
          # The fuzzy finder links the vendored Rust `fff` engine (submodule).
          submodules: recursive

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: cargo-registry-${{ runner.os }}-${{ hashFiles('plugin/third_party/fff/Cargo.lock') }}
          restore-keys: cargo-registry-${{ runner.os }}-

      - name: Cache Cargo build artifacts
        uses: actions/cache@v4
        with:
          # Corrosion writes Rust build artefacts into plugin/build/cargo/ (set via
          # CORROSION_BUILD_DIR in Corrosion.cmake). Without this cache every CI run
          # recompiles ~400 MB of Rust code even when fff source is unchanged.
          path: plugin/build/cargo
          key: cargo-build-${{ runner.os }}-${{ hashFiles('plugin/third_party/fff/Cargo.lock') }}
          restore-keys: cargo-build-${{ runner.os }}-

      - name: Install Qt ${{ env.QT_VERSION }}
        uses: jurplel/install-qt-action@v4
        with:
          version: ${{ env.QT_VERSION }}
          modules: "qtmultimedia"
          cache: true

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            libarchive-dev \
            libfreexl-dev \
            libfreetype-dev \
            libfontconfig1-dev \
            libjpeg-turbo8-dev \
            libopenjp2-7-dev \
            libboost-container-dev \
            pkg-config \
            ninja-build \
            locales
          sudo locale-gen en_US.UTF-8

      - name: Cache KF6 SyntaxHighlighting + QXlsx
        id: deps-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.DEPS_PREFIX }}
          key: deps-${{ env.QT_VERSION }}-kf6${{ env.KF6_SYNTAX_TAG }}-qxlsx${{ env.QXLSX_TAG }}-${{ env.POPPLER_TAG }}

      - name: Build Extra CMake Modules (ECM)
        if: steps.deps-cache.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch ${{ env.KF6_SYNTAX_TAG }} \
            https://invent.kde.org/frameworks/extra-cmake-modules.git
          cmake -G Ninja -B extra-cmake-modules/build -S extra-cmake-modules \
            -DCMAKE_INSTALL_PREFIX=${{ env.DEPS_PREFIX }} \
            -DBUILD_TESTING=OFF
          cmake --build extra-cmake-modules/build
          cmake --install extra-cmake-modules/build

      - name: Build KF6 SyntaxHighlighting
        if: steps.deps-cache.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch ${{ env.KF6_SYNTAX_TAG }} \
            https://invent.kde.org/frameworks/syntax-highlighting.git
          cmake -G Ninja -B syntax-highlighting/build -S syntax-highlighting \
            -DCMAKE_INSTALL_PREFIX=${{ env.DEPS_PREFIX }} \
            -DCMAKE_PREFIX_PATH=${{ env.DEPS_PREFIX }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DBUILD_TESTING=OFF \
            -DQRC_SYNTAX=OFF
          cmake --build syntax-highlighting/build --parallel $(nproc)
          cmake --install syntax-highlighting/build

      - name: Build QXlsx
        if: steps.deps-cache.outputs.cache-hit != 'true'
        run: |
          git clone --depth 1 --branch ${{ env.QXLSX_TAG }} \
            https://github.com/QtExcel/QXlsx.git
          cmake -G Ninja -B QXlsx/build -S QXlsx/QXlsx \
            -DCMAKE_INSTALL_PREFIX=${{ env.DEPS_PREFIX }} \
            -DCMAKE_BUILD_TYPE=Release
          cmake --build QXlsx/build --parallel $(nproc)
          cmake --install QXlsx/build

      - name: Build poppler (Qt6 frontend)
        if: steps.deps-cache.outputs.cache-hit != 'true'
        # Built from source (not Ubuntu's libpoppler-qt6-dev) for the same
        # reason as KF6/QXlsx: the distro package links the SYSTEM Qt6, and
        # mixing it with the install-qt-action Qt ${{ env.QT_VERSION }} would
        # load two Qt copies into one process. Everything non-Qt6 is disabled.
        run: |
          git clone --depth 1 --branch ${{ env.POPPLER_TAG }} \
            https://gitlab.freedesktop.org/poppler/poppler.git
          cmake -G Ninja -B poppler/build -S poppler \
            -DCMAKE_INSTALL_PREFIX=${{ env.DEPS_PREFIX }} \
            -DCMAKE_PREFIX_PATH=${{ env.DEPS_PREFIX }} \
            -DCMAKE_BUILD_TYPE=Release \
            -DENABLE_QT6=ON \
            -DENABLE_QT5=OFF \
            -DENABLE_GLIB=OFF \
            -DENABLE_CPP=OFF \
            -DENABLE_UTILS=OFF \
            -DENABLE_LIBCURL=OFF \
            -DENABLE_NSS3=OFF \
            -DENABLE_GPGME=OFF \
            -DENABLE_LCMS=OFF \
            -DENABLE_LIBTIFF=OFF \
            -DENABLE_QT6_TESTS=OFF \
            -DBUILD_TESTING=OFF \
            -DTESTDATADIR=/tmp
          cmake --build poppler/build --parallel $(nproc)
          cmake --install poppler/build

      - name: Build plugin
        working-directory: plugin
        env:
          LD_LIBRARY_PATH: ${{ github.workspace }}/deps/lib
          # poppler-qt6 is discovered via pkg-config, not find_package — point
          # it at the from-source install in the deps prefix.
          PKG_CONFIG_PATH: ${{ github.workspace }}/deps/lib/pkgconfig
        run: |
          cmake -G Ninja -B build \
            -DCMAKE_INSTALL_PREFIX=/usr \
            -DCMAKE_BUILD_TYPE=Release \
            -DCMAKE_PREFIX_PATH=${{ env.DEPS_PREFIX }}
          cmake --build build --parallel $(nproc)

      - name: Run tests
        working-directory: plugin
        env:
          QT_QPA_PLATFORM: offscreen
          LD_LIBRARY_PATH: ${{ github.workspace }}/deps/lib
          XDG_DATA_DIRS: ${{ github.workspace }}/deps/share:/usr/local/share:/usr/share
          LC_ALL: en_US.UTF-8
        run: ctest --test-dir build --output-on-failure --timeout 60
